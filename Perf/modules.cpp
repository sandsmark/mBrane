
/*
 * this is done because the definition of the modules
 * is subclassed from a template. which can only be in a header
 * 
 */
#include "Perf_modules.h"

LOAD_MODULE(ping)
LOAD_MODULE(pong)
LOAD_MODULE(RRMaster)
LOAD_MODULE(RRModule)
